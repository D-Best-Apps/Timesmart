services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      # args:
      #   CACHE_BUST: ${CACHE_BUST-}
    container_name: timeclock_app
    ports:
      - "8080:80"
    env_file:
      - .env
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - timeclock_network
    # depends_on:
    #   - db

  # db:
  #   image: mariadb:latest
  #   container_name: timeclock_db
  #   restart: unless-stopped
  #   env_file:
  #     - .env
  #   volumes:
  #     - ./timeclock-schema.sql:/docker-entrypoint-initdb.d/init.sql:ro
  #   networks:
  #     - timeclock_network

networks:
  timeclock_network:
    driver: bridge
